import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

void main() {

    /*
    JFrame frame = new JFrame("Random Generator");
    frame.setSize(300, 200);
    frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    frame.setLayout(null);

    JLabel label = new JLabel("Нажми Generate");
    label.setBounds(90, 40, 150, 30);

    JButton button = new JButton("Generate");
    button.setBounds(80, 90, 120, 30);

    Random random = new Random();

    button.addActionListener(e -> {
        int number = random.nextInt(100) + 1; // 1..100
        label.setText(String.valueOf(number));
    });

    frame.add(label);
    frame.add(button);
    frame.setVisible(true);
}
     */


    /**=================================================**/
    /** DataLoader loader =new DataLoader();
     DataLoadedHandler handler = new DataLoadedHandler();
     loader.addDataLoaded(handler);
     loader.loadData();
     loader.removeDataLoader(handler);
     */
/**===================================================*/
    /*JButton button = new JButton("Click");
    JFrame frame =new JFrame("Example");
    JLabel lable = new JLabel("Clicks: 0");
    final int[] count ={0};
    button.addActionListener(new ActionListener() {
        @Override
        public void actionPerformed(ActionEvent e) {
            count[0]++;
            lable.setText("Clicks "+ count[0]);
        }
    });
    frame.setLayout(new FlowLayout());
    frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    frame.add(button);
    frame.add(lable);
    frame.setSize(300,300);
    frame.setVisible(true);

     */
    /*=================================*/
   /* button.addMouseListener(new MouseAdapter() {
        @Override
        public void mouseClicked(MouseEvent e) {
            super.mouseClicked(e);
        }
    });
    */
   /* button.addKeyListener(new KeyAdapter() {
        @Override
        public void keyPressed(KeyEvent e) {
            System.out.println(e.getKeyChar());
        }
    });
    */
    /*============================================*/
}